"""Salary prediction model package.

Modules:
    train  — training pipeline (LightGBM, time-series split, Optuna tuning)
    score  — inference (load saved model, score players, single-player estimates)
"""
